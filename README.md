# TCP-vs.-UDP-Network-Benchmark
 Python benchmarking suite comparing TCP vs. UDP performance across payload sizes and concurrency levels. Measures per-request RTT, throughput, and TCP connection-setup overhead via automated experiments on networked machines, with JSONL logging and 8 auto-generated matplotlib plots across 30 test configurations.
